// all operations in VECTOR

// non manipulating algorithm

#include <bits/stdc++.h>
#include <iostream>
#include <vector>
#include <numeric>

using namespace std;

int main(){

    // init vector with array
    int ar[] = {10, 20, 5, 23, 42, 15};
    int n = sizeof(ar) / sizeof(ar[0]);
    vector<int> vect(ar, ar + n);

     cout << "vivan";
}

