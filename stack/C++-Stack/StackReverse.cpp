#include <bits/stdc++.h>
#include <stack>
#include <string>
using namespace std;

stack<char> st;

// string implementation to store the reversed array
string ns;

char insert_at_bottom(char x){
    if(st.size() == 0)
        st.push(x);
    else
    {
        /* code */
    }
    
}