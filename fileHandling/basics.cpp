// extension of the file is C++ but i will try the basics in C
// reading a file
#include <bits/stdc++.h>
#include <stdio.h>

using namespace std;

int main(void){
    FILE *filepointer;
    filepointer = fopen("filename.txt", "r");
    fscanf(filepointer, "%s %s %s %d", str1, str2, str3, &year);

    FILE *filepointer1;
    filepointer1
}