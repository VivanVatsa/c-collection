#include <bits/stdc++.h>
//in double linked list we add extra pointer previous pointer
struct node{
    int data;
    struct node *next;
    struct node *prev;
};

void push(struct ndoe **head_ref, struct node **previous)
int main(void){

}