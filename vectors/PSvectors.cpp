#include <iostream>
#include <vector>

using namespace std;

int main(){
    vector<int> numbers;
    numbers= {0,1,2};
    numbers.push_back(-3);
    number[0] = 4;
    int num = numbers[2];

    for(int i: numbers){
        cout << i << endl;

    }
}